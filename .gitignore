# Filing PDFs the app downloads (can be 100+ MB; re-downloaded on demand)
downloads/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# Your own settings -- can contain API keys, so never commit it
settings.json

# Streamlit secrets (not used by this app, but never commit them)
.streamlit/secrets.toml

# Editor / assistant tooling and OS files
.claude/
.agents/
.vscode/
.idea/
.DS_Store
Thumbs.db

# The autostart LaunchAgent's log
autostart.log
